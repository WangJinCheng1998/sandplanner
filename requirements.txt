# Core dependencies for SanD-Planner
# Python >= 3.9 and a CUDA-enabled PyTorch build are recommended.

torch>=2.0
numpy
scipy
diffusers>=0.30
accelerate
timm
opencv-python
pillow
imageio
imageio-ffmpeg
matplotlib
tqdm
flask
wandb

# Optional: CuPy GPU-accelerated ESDF (otherwise a CPU EDT fallback is used)
#   cupy-cuda12x      # CuPy build matching your CUDA version
